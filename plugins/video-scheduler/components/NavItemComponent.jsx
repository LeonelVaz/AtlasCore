// video-scheduler/components/NavItemComponent.jsx
// Este archivo podría necesitar transpilación si usas JSX directamente
// Para este ejemplo, proporcionaré la versión React.createElement en index.js
/*
import React from 'react'; // Asumiendo que React está disponible globalmente o provisto por Atlas

function NavItemComponent({ pluginId, onNavigate, pluginName }) {
  const handleClick = () => {
    onNavigate(pluginId, 'main-scheduler-page');
  };

  return (
    <div className="nav-item" onClick={handleClick} style={{ cursor: 'pointer', display: 'flex', alignItems: 'center', padding: '8px' }}>
      <span className="material-icons" style={{ marginRight: '8px' }}>movie_filter</span>
      <span>{pluginName}</span>
    </div>
  );
}
export default NavItemComponent;
*/