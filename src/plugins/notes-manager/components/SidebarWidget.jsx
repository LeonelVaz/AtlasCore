import React from 'react';

export function createSidebarWidget(plugin) {
  // Retornar un componente vacío ya que ahora las notas recientes
  // están integradas en el botón de la toolbar
  return function SidebarWidget(props) {
    return null;
  };
}